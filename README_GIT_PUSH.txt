Git commands to push to GitHub:

```bash
cd "c:/Users/kunal/OneDrive/Desktop/SecureOps TaskFlow API/secureops-taskflow"

git init

git add .

git commit -m "feat: SecureOps TaskFlow API full-stack assignment"

# create branch (optional)
# git checkout -b blackboxai/secureops-taskflow

# add remote
# git remote add origin <YOUR_GITHUB_REPO_URL>

# push
# git branch -M main
# git push -u origin main
```

